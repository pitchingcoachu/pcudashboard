node_modules/
.next/
out/
.env.local
.env*.local
.DS_Store
coverage/
.vercel/
*.log
__pycache__/
*.pyc
.vercel
.motion-capture-uploads/
# Private local exports containing social-account follower data.
private_exports/
# Personal contact data (names/phone numbers) -- never commit.
contacts_names_phone_numbers.csv
# Generated data exports (player/coach/admin rosters, athlete-import
# spreadsheets) -- disposable output containing real people's info, not source.
output/

# Python virtualenv for the dashboard_api service -- machine-specific, regenerable.
dashboard_api/.venv/
/Axioforce/
# Raw training-data parquet caches for Stuff+ 2.0 and Command+ -- large
# (100s of MB), regenerable from source data, not read by the running app
# (only the trained model JSON in dashboard_api/app/stuff2_models/ and
# dashboard_api/app/command_models/ is).
dashboard_api/stuff2_training/data/
dashboard_api/command_training/data/
.env*
