name: PRO AAA Sync (Deprecated)

on:
  workflow_dispatch:
    inputs: {}

concurrency:
  group: pro-sync
  cancel-in-progress: false

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated notice
        run: |
          echo "Deprecated workflow. Use 'PRO Sync' workflow (pro-mlb-sync.yml), which now runs MLB + AAA + Savant enrichment together."
