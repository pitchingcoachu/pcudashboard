# PCU Dashboard

Standalone Next.js + TypeScript website for the PCU Dashboard product.

## Run locally

1. Install dependencies: `npm install`
2. Start dev server: `npm run dev`
3. Build production: `npm run build`

## Notes

- This project is separate from the main `website` project.
- Replace placeholder metrics and links with live dashboard data and auth flow.
